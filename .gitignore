# Operator-only material (Phase 6). Never commit APKs, keys, or pairing captures.
*.apk
*.aab
*.apks
*.xapk
*.dex
*.so
btsnoop_hci.log
*.pcap
*.pcapng
operator_local/
dumps/
candidates.txt
*_candidates.txt
operator_keys/

# Android local SDK / build (GGS Monitor). Captures and APKs stay out of git.
android/**/local.properties
android/**/.gradle/
android/**/build/
android/**/.idea/
android/**/*.iml
android/**/captures/
